#[derive(Debug, PartialEq)]
pub enum Stress {
    High,
    Low,
}

#[derive(Debug, PartialEq)]
pub enum Hunger {
    Very,
    ALittle,
}

#[derive(Debug)]
pub enum Dinner {
    Convinience,
    NabeSet,
    Rice,
}

